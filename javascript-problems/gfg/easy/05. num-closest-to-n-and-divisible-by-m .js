console.log("05 Number closest to n and divisible by m");

const num1 = parseInt(prompt("Enter 1st number"));
const num2 = parseInt(prompt("Enter 2nd number"));

console.log(`Finding Number closest to ${n} and divisble by ${m}`);

function checkPlus(n, m){
    if (((n + 1) % m) == 0) {
        return n+1;
    } 
}

function getResult(n, m) {
    
}

const result = getResult(num1, num2);